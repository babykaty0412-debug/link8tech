name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    env:
      # 產品面向台灣市場，日期測試以台灣時區斷言；CI 機器預設 UTC 會差 8 小時
      TZ: Asia/Taipei
    steps:
      - name: 取出程式碼
        uses: actions/checkout@v4

      - name: 設定 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: 安裝依賴
        run: npm ci

      - name: 單元測試（Vitest）
        run: npm test

      - name: 型別檢查 + 建置
        run: npm run build
